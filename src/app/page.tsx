export default function Home() {
  return (
    <main className="flex flex-1 flex-col items-center justify-center gap-2 bg-[var(--sd-bg)] p-8 text-center">
      <h1 className="text-2xl font-semibold text-[var(--sd-text)]">ShieldDesk</h1>
      <p className="max-w-sm text-sm text-[var(--sd-text-muted)]">
        AI Chat Widget — Testing Stage.
      </p>
    </main>
  );
}
